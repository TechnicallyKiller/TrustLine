# Dependencies
node_modules/

# Secrets — every package's .env, never committed. .env.example IS committed.
.env
.env.*
!.env.example
!.env*.example
.wallets-recovery.json

# Rust / Soroban build output
target/
**/target/
*.wasm

# Node / Next build output
dist/
.next/
out/
*.tsbuildinfo

# Logs & OS cruft
*.log
.DS_Store

# Local Stellar dev skill (kept out of the repo)
.stellar-dev-skill/

# Local script with a testnet API key — never commit
backend/_oz_wire.sh

# Local durable key backups — NEVER commit
.secrets-backup/

# python test venv (agent-sdk-py)
.venv-sdktest/

# Local reference clone of the Tael partner repo (rahulsainlll/tael-protocol) —
# its own git repo, kept alongside for integration work; must never be committed
# into this repo. See TAEL_PARTNERSHIP.md for context.
tael-protocol/
.treasury-wallet.local

# Local wallet key files (demo holding wallets, treasury, etc.) — never commit.
*.local

# SCF submission working draft — internal, not for the public repo
scf45-submission.md
